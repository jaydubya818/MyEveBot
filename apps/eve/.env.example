# Copy to .env.local and fill in real values (never commit .env.local).
# On Vercel, `vercel env pull` writes most of these for you, including the
# VERCEL_OIDC_TOKEN that eve uses to reach the AI Gateway in development.

# --- Personal web access --------------------------------------------------
# Required in production. Use a unique 12+ character password and a random
# 32+ character signing secret (`openssl rand -base64 48`).
MYEVE_ACCESS_PASSWORD=""
MYEVE_SESSION_SECRET=""
# Stable internal subject for the one owner. Do not change after launch.
MYEVE_OWNER_ID="owner"

# Older deployments may still use SOFIE_ACCESS_PASSWORD,
# SOFIE_SESSION_SECRET, and SOFIE_OWNER_ID. They remain supported as
# compatibility aliases; new deployments should use the MYEVE_* names.

# --- Database (Neon Postgres) ---------------------------------------------
# Threads, receipts, reminders, webhooks, automation runs, push subscriptions.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"

# --- Long-term memory (Supermemory) ---------------------------------------
SUPERMEMORY_API_KEY="sm_..."

# --- App integrations (Composio) ------------------------------------------
# Consumer API key used for tool calls and the Connections manager.
COMPOSIO_API_KEY="ck_..."

# --- File sharing (Vercel Blob) -------------------------------------------
# Backs the share_file tool and the chat-created skill store.
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# --- Web push notifications -----------------------------------------------
# Generate a pair with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""

# --- Telegram channel (optional) ------------------------------------------
TELEGRAM_BOT_TOKEN=""
TELEGRAM_WEBHOOK_SECRET_TOKEN=""
# Required in production when Telegram is enabled. Comma-separated Telegram
# user ids allowed to DM the bot; an empty production list denies every sender.
TELEGRAM_ALLOWED_USER_IDS=""

> Latest: [final engineering closure](final-engineering-closure.md). **HOSTED TARGET READY FOR RESOURCE PROVISIONING; overall NO-GO.** Local three-worker simulation and stop PASS. Relay retains direct KMS workload authority under the approved application-admission boundary. KMS provider compatibility and both external gates remain unverified/NOT_RUN. Earlier checkpoints below are historical where superseded.

# Pre-provisioning engineering checkpoint

**NO-GO — EXTERNAL QUALIFICATION PENDING. HOSTED TARGET INCOMPLETE.**

This checkpoint implements additional engineering; it does not satisfy the requested end state. Resource provisioning alone cannot close the remaining application/control integrations. No paid resources, hosted model calls, hosted security tests, or external qualification gates were executed. Federation remains disabled. The frozen MyEve `60d341f9909936f03e4d72e1a7f845721ef82c46` and Relay `614c638d6fc4099db8064540326f5de4438e93a1` pins are unchanged.

1. **Canonical base.** GitHub main was inspected at `7ea29b2886d2b8bad7b1a1ca1c3e8df1d39ee9ee`. The existing four-commit federation series was ported onto that base before the qualification crypto changes. Canonical owner-preview authentication, `iad1`, CI/release controls, production-browser harness and V1 frontier were retained. Canonical main was not modified.

2. **Candidate.** Relay product commit `22ec54c`; separate qualification scripts/docs `33df01d` and `60459e6`. Final candidate `fabe7c010264f89d1397b364583e80b7593f9f59`, branch `codex/myeve-federation-preprovision`, durable checkout `../relay-preprovision-current`. Readiness engineering commits `e241fc3` and `216bc94` contain the controller, local stop/supervision and pinned-model adapter. MyEve application source is unchanged.

3. **Oversized-envelope disposition.** Producer checks the exact canonical compact-JWS signing material before invoking the signer. Oversize fails with deterministic `INVALID_INPUT`/413; no truncation, dropped records, hashing substitution or verifier relaxation. The original schema-valid knowledge join remains reproduced: 88,347-byte submission plus 115,600-byte publication, five records, would produce 270,431 signing bytes / 270,518 token characters. It now rejects before signing. Artifact content remains referenced rather than embedded.

4. **Canonical maximum.** Complete token: 262,144 ASCII characters. Raw Ed25519 signing input: 262,057 bytes. Signature: 64 bytes / 86 base64url characters, plus the final separator. Boundary tests prove a valid exact-maximum token verifies and one signing character beyond is rejected before signing. Additional tests cover small input, escaped JSON and multi-record aggregation.

5. **Google KMS compatibility: UNVERIFIED.** Required request is 262,057 raw bytes / 349,412 base64 characters. API result NOT_RUN; no provider signature/CRC/pinned-key evidence exists. Current [AsymmetricSign API](https://docs.cloud.google.com/kms/docs/reference/rest/v1/projects.locations.keyRings.cryptoKeys.cryptoKeyVersions/asymmetricSign), [RPC contract](https://docs.cloud.google.com/kms/docs/reference/rpc/google.cloud.kms.v1) and [signature guide](https://docs.cloud.google.com/kms/docs/create-validate-signatures) did not establish that maximum. A Google Tink client guard at 64 KiB is a client limitation, not proof of this service's Ed25519 limit. Encrypt/Decrypt and MAC limits are not substituted. `kms-size-probe.ts` is prepared for an explicitly authorized existing key; it creates nothing and has not run. If the actual limit is smaller, stop before further protocol changes.

6. **STS/OIDC.** Implemented request-context Vercel header acquisition, exact team/project/custom-environment/issuer/audience/subject checks, assertion freshness and STS expiry checks, direct STS exchange and KMS composition. No ADC, build-token or service-account-private-key fallback; no global credential cache. Qualification composition requires the exact Relay project and `federation-qualification` environment. Thirteen mocked identity tests pass. Real custom-environment ID and owner GCP selection remain absent; LIVE IAM is not qualified. See [Vercel claims](https://vercel.com/docs/oidc/reference) and [Google STS](https://docs.cloud.google.com/iam/docs/reference/sts/rest/v1/TopLevel/token).

7. **Envelope wrapper.** Implemented remote symmetric KMS wrapping of a fresh 32-byte per-payload DEK. Authenticated context hashes a domain separator, immutable KMS version and owner. Version binding prevents relabeling old ciphertext to evade revocation. Stable logical wrapper ID preserves the existing stored-envelope shape; opaque wrapped-key data carries the KMS version. ACTIVE wraps/decrypts, RETIRED decrypts only while remotely enabled, DISABLED/REVOKED deny. Seven provider-contract tests pass, including wrong owner, rotation, relabeling, disable and IAM denial. No live KMS call occurred.

8. **Purpose and lifecycle.** Existing purpose-separated evidence/delivery/passport signing, exact-version pinning, CRC checks, pinned public verification and historical receipt verification were preserved on the new base. Lease/session signing requires its own configured purpose; no general enablement. Bootstrap needs three asymmetric keys and one symmetric wrapping key. Runtime metadata reads require `cryptoKeyVersions.get` on the corresponding key, now included for wrapping as well.

9. **Controller.** Added authenticated component roles, server-side credential hashes, shared PostgreSQL mutations, operation/body/method/origin-bound one-use 15-second permits, session deadline, heartbeat and stop checks. Application/worker roles must have no `fq_control` mutation privileges. Controller methods and bounded HTTP server are implemented and locally tested. **Not yet wired into every MyEve/Relay origin, KMS call or production startup.** The public origins do not yet prove bypass rejection. This is a material engineering blocker.

10. **HTTP.** Controller counts each attempt, including a retry with a new operation ID, under the durable 2,000 ceiling. Reusing an ID does not invoke upstream again. Two slots remain held through bounded response-body consumption; uncertain transport/body outcomes keep their slot. Tests cover ceiling, single-use permits, stale heartbeat and body completion. Full application/provider interception and control-plane traffic accounting remain to be closed before hosted use.

11. **Artifacts.** Controller validates actual bytes before exposure: 65,536 per artifact, eight artifacts, 524,288 aggregate bytes. Oversized buffers fail before exposure. **Application publication/storage binding and removal of all bypass storage credentials are still incomplete.** The controller test does not certify the live artifact path.

12. **Model liability.** Added direct Claude API adapter pinned to `claude-haiku-4-5-20251001`, standard tier, text-only input up to 16,000 UTF-8 bytes, output ≤800 tokens, no tools/thinking/cache writes/retries. Documented full 200K context at $1/MTok plus 800 output at $5/MTok gives a conservative $0.204 token liability, below $0.25 reserved. The controller durably reserves first and counts the provider HTTP attempt. Unknown cost/response stops the session and retains liability. Existing ledger preserves concurrency one, $2/$3 allocation, $4 new-work stop and $5 maximum. A fresh price review must cover each window. This is not an account-wide billing cap. **Both MyEve paths still need integration and isolation from other model credentials.** [Model limits and pricing](https://platform.claude.com/docs/en/models/haiku-4-5/overview), [pricing policy](https://platform.claude.com/docs/en/about-claude/pricing).

13. **Emergency stop.** Added exact-synthetic-database SQL adapters that revoke Agent credentials/grants and deny nonterminal requests, with outcome reads. Local model disable removes its controller credential. Shared stop orchestrator reports UNCONFIRMED rather than success on partial failures and preserves evidence before DB brake. Real process termination and synthetic SQL tests pass. **Railway deployment-stop verification, provider credential revocation, MyEve in-flight cancellation and persistent evidence sink remain unwired.** Emergency SQL state change is not a normal signed protocol receipt.

14. **Workers.** Process-group supervisor registers the session/SHA, heartbeats every five seconds, stops on heartbeat failure/deadline, sends TERM then KILL within five seconds, does not restart and refuses privileged controller/provider credentials in child environment. Three process tests pass. **The three actual service entrypoints and independently verified checkout SHA are not complete.** Passing a configured SHA is not source attestation. Railway commands remain null to prevent treating a supervisor library as a runnable qualified worker.

15. **Railway containment.** The $0.10 hard-ceiling claim is withdrawn. Proposed three replicas total, each 0.25 vCPU/0.5 GB, ≤3,600 seconds, NEVER restart. Conditional compute estimate $0.041688; 0.05 GB assumed egress adds $0.0025, totaling $0.044188. These are conditional variable-cost estimates, **not an all-in upper bound**: fractional limits, total shutdown, build spend and egress containment are unverified. No shared workspace spend setting was changed. [Scaling](https://docs.railway.com/deployments/scaling), [cost control](https://docs.railway.com/pricing/cost-control), [rates](https://railway.com/pricing).

16. **Region.** MyEve A ↔ Relay ↔ independently operated MyEve B target `iad1`; candidate KMS `us-east4`. This follows current canonical deployment configuration. The previous statement that canonical Relay requests `sfo1` is stale. No region override approval is newly requested.

17. **Browser/performance.** Current canonical production harness replaces the obsolete development-mode performance classification. First loaded run failed `/` at 328.386 ms p95. Isolated rerun passed all nine routes with unchanged 200 ms assertion. Historical warm hosted canonical observations (147.6/169.4 ms) remain historical, not evidence for a newly deployed candidate. Full results are in the evidence record. No threshold was weakened.

18. **Regression.** Relay completion run: **272 passed / 5 skipped**; focused crypto 70 (included), performance 2, functional browser 2, production browser 1 (nine routes, maximum p95 161.382 ms). Readiness controls: **61 passed** (39 PostgreSQL/controller/model/stop/worker, seven existing Node contracts, 15 Python). Typecheck, lint, production build, migration check and V1/V2 frontier pass. See `evidence/preprovisioning/verification.json` for original failures. Full default-timeout runs each had one different five-second DB-test timeout; their failures are retained. The completion run uses a documented 15-second local test timeout, preserving assertions and repository CI settings. Five skipped tests are not live proof. MyEve was not modified and was not requalified this checkpoint. No claim of complete cross-platform qualification is made.

19. **Google manifest/cost.** `preprovisioning-manifest.json` lists region/key ring/four exact key names, algorithms, first versions, WIF names, exact project-level custom-role definitions with key-level grants, claim conditions and audit logging. Project/billing and actual custom-environment ID remain owner inputs. Key estimate $0.24/month initially, $0.36 with two extra retained signing versions; software operations estimated $0.03/10,000, excluding logging/egress/tax. Manifest is `apply:false`; maximum-size acceptance remains a prerequisite.

20. **Railway manifest/cost.** Same machine manifest and `worker-specification.json` identify the workspace and three proposed services. Existing unrelated services are not reused. Exact commands/health wiring and enforceable resource controls are intentionally unresolved rather than fabricated. **This is not a final approvable provisioning manifest.** No provisioning authorization is requested yet.

21. **Remaining owner actions versus engineering.** For size proof, identify an authorized existing GCP Ed25519 key/version and public key through the approved secret channel, or obtain authoritative provider confirmation; resource IDs only in chat. Later: select project/billing, exact resource authorization, independent peer operator/assessor, stop contact and test window. Existing qualification-scoped Vercel storage approval persists. Engineering must still integrate all guarded app paths, eliminate credential bypasses, finish service/stop adapters, validate runtime resource containment and rerun end-to-end local tests. Owner approval cannot substitute for that work.

22. **Independent security: NOT_RUN.** The assessor must receive a frozen source/target package from a separate context. Add maximum envelope, owner/version AAD substitution, OIDC wrong environment, one-use permit/retry exhaustion, stale heartbeat, direct-origin/model/storage bypass and partial-stop cases to `security-review.md`'s target. Do not execute this target yet. Implementation agent cannot mark this gate PASS.

23. **Production platform: NOT_RUN.** Keep existing runbook coverage for identity/registration, explicit publication/private isolation, knowledge query, signed/replayed delivery, grants/publication revocation, messaging/work, local-authority refusal, safe execution/artifact transfer, recovery, credential rotation/revocation and audit/provenance. Hosted MyEve B is a separately operated real receiver/executor, unlike disposable Ava; it is not heterogeneous-vendor certification. No qualified hosted deployment IDs exist yet.

24. **Final verdict.** NO-GO — EXTERNAL QUALIFICATION PENDING. HOSTED TARGET INCOMPLETE. The envelope defect and crypto/controller components advanced, but this mission did not eliminate all engineering blockers. No paid provisioning, external gate, general enablement, canonical merge or frozen-pin promotion.

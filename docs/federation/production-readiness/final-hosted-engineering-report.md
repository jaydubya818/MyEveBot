# Final hosted engineering checkpoint

**NO-GO — EXTERNAL QUALIFICATION PENDING / HOSTED TARGET INCOMPLETE.**

The requested provisioning-ready outcome has **not** been achieved. Application integration and component tests advanced, but exclusive KMS custody and the assembled three-worker topology remain unresolved. No paid infrastructure was provisioned, no hosted qualification ran, and federation remains disabled by default. Do not authorize provisioning on the strength of passing component tests.

This report supersedes earlier implementation-status claims where they conflict. Frozen release pins remain MyEve `60d341f9909936f03e4d72e1a7f845721ef82c46` and Relay `614c638d6fc4099db8064540326f5de4438e93a1`.

1. **Candidate source.** Relay `894e6a3f5e99c2b186f2cfa72743d244d51a2c98`, based on the existing `fabe7c0` candidate over canonical `7ea29b2886d2b8bad7b1a1ca1c3e8df1d39ee9ee`. MyEve `2ae91a8095db162188046a5c8fa6d987571e9933`. Controls `39f0bd22194f8b07c97e71377dcc17117ac23b6a`. These are engineering candidates, not promoted release pins. Canonical repositories were not merged or modified.
2. **Controller interception.** MyEve's real Relay client, artifact transport and work model branch now use authenticated controller RPC in explicit qualification mode. Relay qualification STS/KMS transport also uses it. Ingress claims bind operation, method, exact URL, body digest, origin and live worker; 15-second permits are one-use. Sessions are checked at the RPC boundary. Controller is the sole intended runtime ledger writer; hosted role grants are not yet installed. Response bodies are bounded before release; ambiguous HTTP attempts retain their slots. This closes normal application call paths, not exclusive custody of the Vercel workload identity.
3. **Direct-origin/provider bypass.** Local client/handler tests deny missing controller, forged/direct ingress, wrong owner, unlisted provider routes and fallback credentials. However, Relay's Vercel process can obtain the OIDC assertion that Google trusts. It therefore has the capability to exchange that assertion and contact KMS without this application controller. No provider-side denial has been demonstrated. Treat the direct-provider requirement as **UNCLOSED**, not PASS. No unauthorized provider call was attempted.
4. **Artifacts.** Real receive, work-output, publication and retrieval paths request byte admission. The owner-bound storage handler separately consumes its ingress permit and rechecks decrypted content before SQL. Immutable IDs permit retries only for matching content; exposure gets a fresh operation. Durable accounting caps actual content at 65,536 bytes each, eight allocations and 524,288 aggregate bytes, including stored copies in distinct owners. Worker startup rejects artifact INSERT/UPDATE/TRUNCATE privileges; workers use the guarded web storage endpoint. Dedicated worker-role grant preparation and assembled validation remain outstanding.
5. **Haiku/MyEve.** Actual `executeExternalWork` now calls the controller's pinned Haiku adapter during qualification, preserving local ActionGateway authority consumption and the fresh Relay check. The normal application model path is unchanged. Workers receive no model API key. The controller pins `claude-haiku-4-5-20251001`, standard tier, 16,000 input bytes, 800 output tokens, no tools/thinking/cache writes and no provider retries. The full real-work path has not yet run in the assembled target.
6. **Liability.** Full documented 200,000-token context at $1/MTok plus 800 output tokens at $5/MTok yields $0.204 maximum token liability, below a durable $0.25 reservation. One concurrent model; $2 A/$3 B allocations; stop new calls at $4 reserved; $5 absolute ceiling. Reservations precede invocation and are never refunded on cancellation, uncertainty or restart. Provider attempts consume HTTP admission and a request-bound consumed permit. Unknown usage fences the call and stops the session. Current price review is required for each <=1-hour window. This is qualification-session liability accounting, not an account-wide provider billing cap.
7. **Workers.** Prepared executable supervisor command: `node qualification-runtime/worker-entrypoint.mjs`, with frozen `FQ_COMPONENT=myeve|relay|peer` and exact source SHA. MyEve A/B execute `apps/eve/scripts/qualification/myeve-worker.ts`; Relay executes `scripts/qualification/relay-maintenance.ts`. Source export verifies file hashes; Git checkouts reject tracked modifications. Heartbeat every five seconds, stale after ten seconds; authority checked before polls; process-group TERM then KILL within five seconds; no automatic restart. Admission ends at 45 minutes and absolute lifetime is <=60 minutes. Packaging and all-three-worker execution remain NOT_RUN.
8. **Emergency stop.** The operator-side composition closes durable admission, revokes exact synthetic credentials and grants, denies nonterminal work, disables controller model authority, attempts all three exact Railway deployment stops, preserves redacted evidence in PostgreSQL, then applies NOLOGIN/password clearing/connection termination to application and configured worker roles. Each step returns VERIFIED or UNCONFIRMED; any partial failure prevents complete=true. SQL, process termination, partial failure, timeouts and Railway request/readback components are tested. Live Railway schema was inspected read-only; no stop mutation was sent. The full hosted composition and deployment-replacement race still need validation.
9. **Bypass audit.** This is implementation self-audit, not independent security assessment. See the matrix below. New findings prevent a provisioning-ready verdict.
10. **Local three-party simulation.** **NOT_RUN.** Existing PostgreSQL, model-contract, artifact-handler and real process-supervisor tests are not a substitute for MyEve A → Relay → MyEve B through three actual supervised application workers. Do not reuse the old disposable Ava evidence as proof of this target.
11. **Railway manifest.** `preprovisioning-manifest.json` records three executable packaged commands, source pins, one replica, proposed 0.25 vCPU/0.5 GB each, no volumes, NEVER restart, `/health`, heartbeat and lifetime limits, and exact-ID stop adapter. Those provider resource fractions remain subject to provider acceptance/readback. The $0.044188 conditional compute/egress estimate is **not** a hard cap; builds, additional egress, subscription and tax are excluded. The $0.10 hard-cap claim remains withdrawn. The manifest is explicitly INCOMPLETE_DO_NOT_PROVISION.
12. **KMS compatibility.** **GOOGLE KMS PAYLOAD COMPATIBILITY = UNVERIFIED.** No authorized existing project/key reference or usable GCP context was supplied; no new key was created. The existing bounded probe remains ready for 262,057 raw bytes / 349,412 base64 bytes, requiring API result, 64-byte signature, CRC and pinned-public-key verification. Owner must identify/authorize an existing compatible key or approve the exact future resource before the provider test.
13. **Performance/browser.** Two performance tests and two functional browser tests passed. Production browser test passed on all nine routes, 20 samples each; maximum p95 129.159792 ms. The <200 ms threshold is unchanged. Both production builds passed. This measures normal local production builds, not hosted KMS or controller latency.
14. **Regressions.** Relay: **279 passed, 5 skipped**, 51 passed files/3 skipped files. MyEve Vitest: **508 passed**, 76 files; MyEve Node: **130 passed**. Controls Node: **54 passed**; Python controls: **15 passed**. Disabled-default owner/artifact/client/worker probes: **25 passed**. Both typechecks, Relay lint, MyEve executor governance (506 sources, UNKNOWN=0), Relay migration consistency and V1/V2 frontier passed. MyEve ordered migration validation: 27; disposable migration qualification passed all eight checks, including fresh/upgrade, rerun, checksum rejection, rollback and existing execution/action integration scripts. Raw evidence and migration-run details are under `evidence/final-hosted-engineering`. Relay database tests retain the previously documented 15-second test timeout; performance thresholds are unchanged.
15. **Engineering commits.** MyEve `288d9bc` is the separate preexisting WebSocket/ArrayBuffer type compatibility fix; `0953304` integrates qualification transport/model/artifacts and tests; `2ae91a8` adds worker scaffolding. Relay `bbbcc23` integrates qualification ingress/provider transport and tests; `894e6a3` adds worker scaffolding. Readiness `38ddb40` contains controller/runtime/stop engineering; `39f0bd2` adds strict principal/worker configuration checks. Documentation/evidence is committed separately. Durable copies are `myeve-hosted-controls-current` and `relay-preprovision-current` beside this readiness repository.
16. **Remaining owner actions.** First decide whether to revise the previously required Vercel → STS custody design so only a controller identity can obtain KMS authority. This decision is pending; no approval is inferred. A bounded alternative is an operator-issued short-lived KMS credential held only in the controller for the synthetic session, without a service-account private key; its exact IAM and bootstrap design must be reviewed before implementation/provisioning. If the current Vercel custody requirement remains, the direct-provider guarantee remains unresolved. After source work and local topology proof close: select GCP project/billing; authorize exact KMS/IAM/identity resources and three Railway services; approve isolated role grants and deployment configuration; provide assessor, separate peer operator, stop contact and window; authorize provisioning/deployment, smoke and later external gates. These later approvals are not requested by this incomplete manifest.
17. **Independent security: NOT_RUN.** The assessor must receive immutable source/package hashes, frozen target IDs and the existing `security-review.md` rules from a separate context. This implementation agent cannot mark the gate PASS. No package was sent and no penetration test ran.
18. **Production-platform qualification: NOT_RUN.** Target remains independently operated hosted MyEve B as the complete existing receiver, hosted MyEve A and hosted Relay, backed by three isolated synthetic Neon databases and supervised workers. Existing Vercel projects/regions are reused; no private production tenant data is a fixture. See the unchanged full golden-path runbook for identity, registration/publication, private isolation, queries, delivery/replay, revocations, messaging/work, local refusal, safe execution, artifacts, recovery, rotation and provenance. This is not heterogeneous-vendor certification.
19. **Verdict: NO-GO — EXTERNAL QUALIFICATION PENDING / HOSTED TARGET INCOMPLETE.** No paid provisioning or external gate may proceed from this checkpoint.

## Self-audit matrix and unresolved engineering

| Boundary | Evidence/status |
|---|---|
| Forged/reused/expired/wrong-operation permit | Local PostgreSQL controller tests PASS |
| Wrong origin/worker role, stale heartbeat, stopped session, DB unavailable | Local controller/source/supervisor tests PASS |
| Missing controller or worker model/storage bypass credentials | Local client and startup guards PASS; hosted credentials/roles not yet assembled |
| Direct artifact storage handler, wrong owner, permission denial | Real handler component tests PASS; full transfer NOT_RUN |
| Exact/+1 artifact size, concurrent allocation, immutable retry, restart | Local controller/client tests PASS; cancellation after actual hosted storage NOT_RUN |
| Model reservation/concurrency/restart/uncertainty/$5 bound | Local PostgreSQL + pinned-adapter tests PASS; full actual MyEve work simulation NOT_RUN |
| Direct KMS provider access using Vercel identity | ARCHITECTURE UNCLOSED; no exclusive custody or provider-side rejection proof |
| Nested owner HTTP → Relay → KMS | INCOMPLETE: two active origin requests could starve provider admission; the implemented one-origin lane reserves provider capacity but does not support the complete nested owner path. Freeze a worker-driven golden path or otherwise resolve without weakening concurrency/semantics. |
| Controller route/principal configuration, role grants, packages | Prepared modules; complete frozen configuration and assembled execution NOT_RUN |
| Emergency provider stop and evidence before final brake | Component tests PASS; assembled stop and replacement-deployment exclusion NOT_RUN |

The five Relay skips are: Playwright provider context isolation/contract, private-network denial and TTL closure (3); Docker sandbox execution/file isolation/timeout/destruction (1); combined Relay-managed browser/shell/file lifecycle (1). Their explicit `RELAY_LIVE_PLAYWRIGHT` / `RELAY_LIVE_DOCKER` switches were not enabled. Functional/production dashboard browser tests are separate and did run. No skip is represented as provider or external-gate proof.

Existing resource inventory was not reprovisioned or refreshed wholesale in this turn: the three previously created Neon databases, disabled synthetic Vercel branch secret slots and reusable MyEve/Relay projects remain the proposed substrate. Only Railway API schema introspection was performed externally in this turn. No existing owner/private data was queried for test fixtures.

Railway stop operations and terminal-state handling follow the [official API documentation](https://docs.railway.com/integrations/api/manage-deployments) and the sanitized live introspection in this evidence directory. The API returning true is insufficient: the adapter requires exact scope plus terminal readback, otherwise UNCONFIRMED.

# Qualification deployment target — 2026-09-20

**NO-GO — EXTERNAL QUALIFICATION PENDING.** This is a concrete target design and read-only inventory, not a deployed target or gate result. No infrastructure, credential, account, flag, domain or database was created/changed.

## Current deployment evidence

Authenticated Vercel GET requests are captured in [hosting-inventory.json](evidence/hosting-inventory.json). The collector retains allowlisted metadata and environment **names/scopes only**, never values. No owner application data or database rows were read.

| Component | Observed existing deployment | Fitness for qualification |
| --- | --- | --- |
| MyEve/Sofie | Vercel `sofie-personal-agent`, project `prj_L6faw25wnFGUZtrLKBIccg8gIDLR`, root `apps/eve`, Node 24.x; production `dpl_CjbdgUWpszU2nV8srA2m2egmoKgY`, Ready, SHA `1d20be474e7841871001cf592b8f523a178770e1`; domain `sofie-personal-agent.vercel.app` | Existing personal-owner production deployment is excluded from test traffic/data. Do not infer identity with the pinned candidate from a different SHA. |
| Exact MyEve pin | Ready preview `dpl_95jNGPuqEGhuBe2QXpkcUnh8v9U7`, `sofie-personal-agent-2fbhzhmq6-jaydubya818.vercel.app`, SHA `60d341f9909936f03e4d72e1a7f845721ef82c46`, branch `codex/openbot` | Build/hosting can be reused. Current preview is NOT verified synthetic-only. Configure a dedicated qualification branch/environment and redeploy the same source tree if existing preview settings cannot be isolated. Do not repurpose the active `codex/openbot` branch. |
| Relay | Vercel project `relay`, `prj_3IRvr9knK5VJcBTgTYMvhv6ixmJK`; production `dpl_4xD4Gxth8qyZiXfRRSmXSsvHCXLJ`, Ready, SHA `46fdf9bc6aa247811d7ed4b04fd9fc944c41c879`, Node 24.x | Not the federation pin. Historical documentation calls it an actions-disabled owner-only private preview; current secret/config values were not read. Leave that deployment unchanged. |
| Relay domain/preview | Current project domain API lists `relay-sage-nine.vercel.app`; immutable production host is `relay-c5eosdc8p-jaydubya818.vercel.app`. Prior docs name `relay-jaydubya818.vercel.app`. Latest inspected preview `dpl_787DTV4K82jzTm5L2k1GriWYTEP8` is SHA `2405e33eca5131d846044c2c811c15126b2d39a2` on `codex/relay-owner-preview-qualification` | Use deployment IDs as authority; do not assume old aliases route to the pin. No pinned Relay deployment was identified in the inspected latest ten deployments. Do not disturb the concurrent owner-preview work. |
| Relay PostgreSQL | Existing available Vercel Marketplace Neon resource `store_NoIIiEgH5AoDvwL5`, `neon-cerise-car`, plan `free_v3` / Free | Reuse provider resource if it permits empty, independently credentialed logical databases. Historical evidence: PG 18.6, logical restore passed, managed deletion protection unresolved. Current database contents/version/PITR were not queried. |
| MyEve PostgreSQL/Blob | Vercel metadata includes Neon connection settings across production/preview/development and Blob credentials; MyEve README describes Neon and private Blob | Existing integrations are reusable, but no empty qualification database/storage namespace was verified. Never inherit personal data or connector credentials. |
| Ingress | Both projects report `ssoProtection.deploymentType=all_except_custom_domains`; project domain listings show only Vercel domains | TLS hosting exists. Machine-to-machine admission for a synthetic target is unresolved. No bypass token or exception was created. |
| Worker/key service | MyEve and Relay worker source exists. Relay historical evidence says no persistent hosted worker. No existing worker host or production signer/wrapper integration was established by the inspected records | Operator inventory required before purchase. Do not claim that absence in these records proves none exists elsewhere. |

The MyEve and Relay project-level database entries apply to production, preview and development without branch overrides in this inventory. That is evidence of shared configuration scope, not proof of physical database equality. There is no evidence making an existing preview safe to use as-is. There are no `MYEVE_RELAY_*` or `RELAY_FEDERATION_ENABLED` entries in the inspected project env-name lists; this does not attest immutable deployment settings or external worker environments.

## Smallest topology

```text
Real Vercel MyEve/Sofie qualification preview — unchanged MyEve pin
  synthetic single owner; empty MyEve logical PostgreSQL database
  existing relay-worker.ts in a supervised, separately credentialed process
              ↕ actual HTTPS REST + source artifact endpoint
Real Vercel Relay qualification preview — unchanged Relay pin
  empty Relay logical PostgreSQL database; synthetic accounts only
  production signer/key wrapper + startup bindings; maintenance worker
              ↕ actual /api/v2/federation/mcp over HTTPS
Real Codex CLI 0.153.3 — independent operator/session/OS isolation
  dedicated synthetic Agent bearer; no implementation conversation or owner files
  existing federation receiver required for incoming requests (not yet found)
```

Reuse the **two existing Vercel projects, Git/build integration, provider accounts and TLS service**. Create no new web project, purchased domain, Kubernetes cluster, queue or general Agent platform. Qualification requires two independently credentialed empty logical databases, preferably inside already-owned Neon resources. They must be schema-only fresh databases, NOT branches cloned from owner data. If provider permissions or project-wide protection/secrets prevent isolation, stop and obtain a decision before a dedicated project/resource is provisioned. Reuse an existing empty database if operators identify one and attest its isolation; none is verified yet.

MyEve authentication is deployment-scoped to a configured owner (`apps/eve/lib/web-auth.ts`). Its federation flag and Relay's action/federation flags are deployment-wide, not tenant rollout allowlists. Therefore a synthetic tenant inside the active personal MyEve deployment is not the selected approach. A separate preview configuration under the existing project is necessary, with only synthetic tenants reachable in that deployment. Never turn on a global flag in either current production deployment.

## Peer decision

**Recommend Codex CLI as the real peer client, conditional on closing the receiver gap below.** Installed versions: Codex 0.153.3; Claude Code 2.1.270. Sanitized local config inspection found Codex `relay-v1-continuity` and Claude `relay-v1-qualification`, both pointed at localhost `/mcp`. Their existing secrets/identities are excluded from this mission.

| Candidate | Existing evidence | Remaining boundary |
| --- | --- | --- |
| Codex | Relay `docs/v1-release.md` records real `codex exec` shared-memory and isolation flows; installed version matches the recorded 0.153.3. HTTP MCP supports environment bearer/header configuration. TypeScript V2 reference client exercised locally. | Existing V1 `/mcp` is not federation `/api/v2/federation/mcp`. No named-product federation certification or complete Codex receiving adapter identified. |
| Claude Code | Same V1 release evidence records real Claude MCP calls and cross-client flows; installed CLI is newer than recorded 2.1.81. HTTP MCP supports environment-expanded headers. | Same receiver gap; V2 Python client has syntax/contract evidence, not a live product qualification. |

Codex offers the shortest reuse path through the already exercised TypeScript ecosystem and existing real client. Selection is an engineering recommendation, not vendor certification. Configure a fresh isolated profile; do not change existing V1 integration or reuse its credentials. Official client transport references: [Codex MCP](https://learn.chatgpt.com/docs/extend/mcp?surface=cli), [Claude Code MCP](https://code.claude.com/docs/en/mcp).

**A raw MCP client is not a complete federation platform.** The pinned MCP `poll` tool returns signed deliveries; the model must not be asked to verify cryptography or remember replay claims. A receiving integration must already enforce `verifyDelivery`, durable atomic claims across restart, publication-only reads, independent local work policy and recipient artifact proofs. The reusable primitives exist in Relay `lib/v2/federation/transport.ts` and `platform-adapter.ts`; neither CLI configuration supplies their storage/runtime implementation. A requester-only Codex run can exercise hosted MyEve's receiving boundaries, but cannot close the full bidirectional P05/P06/P11 or independent receiver/context-isolation requirements. Do not substitute the Ava harness, a reference-client label, manual model judgement, or an invented PASS. Obtain an existing operator-owned receiver integration or a separate scoped decision for qualification glue; no new receiver was built here.

Meaningful difference from Ava: actual Vercel Next ingress/auth, immutable deployed source, managed PostgreSQL, production key custody, independently operated real Codex binary/session/provider, actual peer runtime restrictions, supervised restart and retained telemetry. The earlier localhost TLS servers, local PostgreSQL and disposable Ava service are not reused as evidence for these properties. If any required property still uses a shim, the corresponding gate stays open.

## Runtime/resource requirements and provisioning delta

| Item | Exact requirement | Existing / required action |
| --- | --- | --- |
| MyEve web | Pin `60d341f…`; Next 16, Node 24; root `apps/eve`; synthetic signed owner auth; HTTPS origins exact; normal 27-migration runner through `0027_relay_federation.sql` | Exact-pin preview exists. Need isolated env and schema-only DB before any opt-in/redeploy. All production data connectors, Supermemory, channels, cron integrations and personal Blob access excluded. |
| Relay web | Pin `614c638…`; production Next routes and owner login/session; `pnpm build/start`; normal Drizzle journal with 22 entries through `0021_violet_captain_stacy` | Hosting exists; pinned qualified deployment not verified. Select existing hosted Node 24.x (pinned package supports >=22.5.0), record exact patch version at freeze and test that build. Docs/worker image use Node 22; record this difference and the independently pinned worker image, rather than assuming identical runtime qualification. Do not change project-wide runtime settings. |
| Relay deployment policy | Federation routes call `requireRuntimeActionsEnabled`; private-preview always refuses; opted-in isolated deployment needs existing non-private-preview mode plus action/federation flags | No synthetic-only profile exists. Propose existing `production` application mode **only on segregated qualification preview**, with global public production untouched; operator approval required. Mode name is not release approval. |
| Relay startup bindings | Every serving process must install `AuditSigner`, trusted `LeaseKeyResolver`, owner-bound `KeyWrapper`, exact issuer through `configureV2PlatformBindings` | No production initializer found at pin or current canonical source. Only qualification shim calls it. Environment flags are insufficient. Need an existing deployment-owned initializer/provider implementation or separately authorized infrastructure wiring, frozen and reviewed; no product-route/semantic change here. |
| PostgreSQL | Two distinct empty databases/roles, TLS, limited connection pools, migration/backup evidence; MyEve Neon HTTP compatibility; Relay direct PostgreSQL | Reuse existing provider resources subject to capacity/privileges. Empty logical databases and least-privilege roles unverified/missing. No plan upgrade authorized. Free-tier protection limitations remain documented; no production durability PASS from a synthetic run. |
| Ingress/TLS | Stable exact HTTPS origins for MyEve/Relay/peer artifacts; no redirect, query secret or public owner endpoint workaround; synthetic-only access | TLS exists. Pinned MyEve client sends no Vercel automation-bypass header and rejects origin query/path; artifact fetch also lacks it. A Codex header alone cannot solve MyEve→Relay. Operator must approve a target-specific ingress exception or existing restricted ingress that serves unmodified clients, preserving other deployment protection. If unavailable, stop before proxy/new project procurement. [Vercel protection](https://vercel.com/docs/deployment-protection/methods-to-bypass-deployment-protection/protection-bypass-automation). |
| Workers | MyEve single serial poller (5-second interval), Relay `pnpm worker` (30-second maintenance), two-worker duplicate test only within budget; process supervisor/logs/stop control | Source exists; no verified hosted supervisor. First ask operators for an existing host. A supervised local machine is useful preflight but cannot attest hosted restart/availability. No Railway/VM created. |
| Signing/custody | Relay persistent Ed25519 AuditSigner + versioned key resolver; owner-bound wrapping and recovery; MyEve separate 32-byte encryption key and Ed25519 artifact key; peer recipient key | Existing interfaces only. Relay local signer/wrapper factories reject NODE_ENV=production. Use approved compatible KMS/HSM service via provider boundary, not generated ephemeral development keys. Existing service/access not identified; purchase/implementation is a stop point. Single key-version unseal/history limitations must be tested, not changed. |
| Secrets | Branch/deployment-scoped owner sessions, DB roles, Agent credentials, issuer pins and model budget key; peer credentials delivered out-of-band; fresh synthetic values | Vercel encrypted/sensitive settings exist. Need isolated scoped credentials and operator-approved custody. No values in scaffolding; secret references only. Do not inherit project defaults without inventory. |
| Peer runtime | Codex 0.153.3 in separate account/container or existing runner, only test workspace, no owner HOME/MCP/provider credentials, no unrestricted tools; pinned receiver if available | Binary/integration pattern exists; isolated runtime/profile/receiver not provisioned. CLI alone needs no inbound port for outgoing MCP. Source-owned peer artifacts require an existing HTTPS endpoint if testing reverse transfer. |
| Telemetry/audit | Structured redacted request/state/cost/attempt logs; signed Relay audit bundle; MyEve local Run/approval evidence; provider usage and operator stop logs; receipt key history | Source audit tables/export helpers exist, Vercel deployment metadata available. Need log access/export destination, owner-filtered signed audit export procedure and redaction verification. No new audit API. |
| Artifacts | ≤64 KiB synthetic UTF-8 text/JSON; MyEve encrypted `myeve_relay_artifacts` DB rows served by existing signed route; recipient proof, size/hash/type | Federation does not require another object bucket for MyEve artifacts. Do not use personal Blob. Peer existing proof-enforcing HTTPS artifact source is required for reverse transfer; not found. Relay evidence object storage needed only if retaining provider event payload bodies; those are excluded. |

## Provisioning decision

Reuse first: existing Vercel projects, exact-pin MyEve build, managed TLS, Neon accounts/resources, real Codex installation, existing protocol and worker code. No additional web platform is justified yet.

Genuinely unresolved: isolated DB roles/databases; pinned Relay deployment with real bindings; synthetic branch envs/identity setup; ingress machine access; compatible key custody/recovery; supervised workers; hard spend/request controls; independent peer receiver/artifact source; operator telemetry access. Some may already exist outside inspected records. Confirm with operators before provisioning anything. The target is not executable merely because Vercel reports Ready.

## Deployment configuration contract

MyEve synthetic branch only: `MYEVE_OWNER_ID`, `MYEVE_ACCESS_PASSWORD`, `MYEVE_SESSION_SECRET`, isolated `DATABASE_URL`; after authorization only, `MYEVE_RELAY_ENABLED=true`, `MYEVE_RELAY_ORIGIN`, `MYEVE_RELAY_OWNER_ORIGIN`, `MYEVE_RELAY_KEY_ID`, `MYEVE_RELAY_PUBLIC_KEY`, `MYEVE_RELAY_ENCRYPTION_KEY`, `MYEVE_RELAY_ARTIFACT_PRIVATE_KEY`, `MYEVE_RELAY_ARTIFACT_ORIGIN`. Configure the normal AI Gateway access mechanism with isolated budget authority. No personal channels, Composio, Supermemory or Blob credentials. Start worker with the same synthetic owner/database/key configuration. All defaults remain disabled in repository/config templates.

Relay synthetic branch only: isolated `RELAY_DATABASE_URL` (takes precedence) or `DATABASE_URL`, fresh `RELAY_SESSION_SECRET` and `RELAY_ENCRYPTION_KEY`, exact `NEXT_PUBLIC_RELAY_URL` and `RELAY_ISSUER_URL`, `RELAY_ALLOW_SIGNUP=false` after controlled synthetic owner creation; only after authorization, `RELAY_DEPLOYMENT_MODE=production`, `RELAY_V2_ACTIONS_ENABLED=true`, `RELAY_FEDERATION_ENABLED=true`. Set `RELAY_WORKER_INTERVAL_MS=30000`. There are no documented environment variables that automatically install the required production `AuditSigner`/`KeyWrapper`; do not invent them. Freeze that deployment-owned implementation and its secret references separately when available.

Secrets must be branch-specific overrides; Vercel project-wide values apply broadly. A new deployment is required for changed environment configuration ([Vercel environment variables](https://vercel.com/docs/environment-variables)). Prevent qualification branch pushes from aliasing production. Audit resolved variable **names and secret references**, including inherited aliases, before deploying; presence of a fresh DATABASE_URL does not justify retaining unrelated production connection variables. Limit database credentials to the two synthetic databases and disable real external integrations by omission/approved existing configuration.

## Exact owner/operator actions still required — stop boundary

1. Approve the proposed reuse of the existing projects with **new isolated qualification branch configurations**, explicitly excluding existing production aliases and concurrent owner-preview work. Confirm no current empty synthetic environment already satisfies these requirements before creating one.
2. Identify existing empty DBs, supervisor host and compatible key service. Authorize only the missing empty logical databases/roles/secrets and exact pinned preview deployment. If these require a new paid resource, project, plan or domain, approve that concrete purchase separately; none is authorized here.
3. Supply an existing production cryptographic initializer/provider implementation, or authorize a separately reviewed infrastructure-only bootstrap to the existing interfaces. Under the present pins the target is blocked without this. Any application source change requires a new reviewed candidate; do not silently amend Relay or widen federation behavior.
4. Approve a synthetic-target-only machine ingress mechanism that works for unmodified MyEve REST/artifact requests. No project-wide protection removal or credential in URL. If the current plan cannot isolate the exception, decide the minimal alternate deployment after its exact cost/scope is known.
5. Identify/provide the independently operated Codex receiver/projection/artifact integration, or decide separately whether bounded qualification glue is allowed. Requester-only CLI compatibility cannot close the full production gate.
6. Assign a peer operator and independent assessor in separate contexts; deliver only frozen source/target package and out-of-band test credential references. Explicitly authorize package dispatch; no message has been sent.
7. Approve each gate's exact targets, synthetic identities, window, budget, traffic watchdog/provider cap, retention and stop contacts. Authorize synthetic owner/account provisioning through existing supported flows and the selected isolated deployment mode; do not weaken current private-preview guard.
8. Verify all prerequisites and freeze deployment/config/receiver hashes before independently authorizing either external test session. The current implementation task stops here; it does not purchase, deploy, enroll, enable, test externally or sign off a gate.
